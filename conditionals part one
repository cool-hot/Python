 def plus(a, b):
  if type(b) is str:
    return None
  else:
    return a + b
  
  print(plus(10,"20"))

def plus(a, b):
  if type(b) is int or type(b) is float:
    return a + b
  else:
    return None

  print(plus(12, 1.2))
 
